<tr>
  <td><%= user.email %></td>
  <td><%= user.full_name %></td>
  <td><%= l user.created_at %></td>
  <td><%= l(user.last_logon.created_at) unless user.logons.empty? %></td>
  <td><%= "Y" if user.confirmed_at %></td>
  <td>
    <%= edit_link user %>
  </td>
</tr>
