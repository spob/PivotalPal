<tr style="font-weight:800;">
  <td style="padding-left: 14px;"><%= task.display_description %></td>
  <%= task.display_status %>
  <td><%= '%.2f' % task.total_hours %></td>
  <% (1..iteration.calc_day_number).each do |d| %>
      <%= task.display_estimate(d, story) %>
  <% end %>
</tr>
