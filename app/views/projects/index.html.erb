<% title "List Projects" %>

<%= paginate @projects %>
<table>
  <tr>
    <th>Name</th>
    <th>Action</th>
  </tr>

  <%= render @projects %>
</table>

<br/>

<% if can? :create, Project %>
    <%= link_to 'New Project', new_project_path %>
<% end %>
