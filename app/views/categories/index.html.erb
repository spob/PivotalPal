<% title "List Categories" %>

<%= paginate @categories %>
<table>
  <tr>
    <th>Name</th>
    <th>Action</th>
  </tr>

  <%= render @categories %>
</table>

<br />

<%= link_to 'New Category', new_category_path %>
